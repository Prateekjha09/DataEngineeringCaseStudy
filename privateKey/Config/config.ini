[CONNECTION_NAME]
ACCOUNT = # this will contain the account name along with region where your snowflake instance is located
USER = # user / admin account email id
ROLE = # will contain for role name which will be assigned to this user
WAREHOUSE = # this will contain the warehouse name which will be utilized for the computation of data fetch and processing
LOG_SIZE =  # contains size of log file which will be generated for entire code run
DATABASE = # this will contain the database name which is created in snowflake instance used
SCHEMA = # this schema inside the above mentioned database will contain all tables which will be used for data dumping and cleaning